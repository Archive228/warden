# Project: warden — agentic launch-vetting tool
# Last updated: 2026-09-06 by session 9 (backtest mode - all 7 features now attempted)

## What's done
- On-chain reader [0], repo fingerprint [1], multi-launchpad [5] — passes:true,
  all independently verified against live chain state.
- Judge [2], alerts/watch [3], execution gate [4], backtest [6] — fully
  implemented, all passes:false, all genuinely blocked (not neglected) on the
  same missing credential: ANTHROPIC_API_KEY. Execution [4] carries an
  additional, deliberate, principle-based block (no real money spent to
  verify a money-moving feature) on top of that.
- Backtest [feature_list.json index: 6] this session:
  - src/backtest/discover.ts: findHistoricalLaunches walks TokenLaunched
    events backward from latest in 5000-block chunks (this factory's activity
    can exceed a single eth_getLogs call's result ceiling over a wide range -
    hit `-32005 limit exceeded` querying it directly back in session 3),
    stopping once a chunk's own block timestamp predates `--since`, not on a
    fixed block-count estimate (block time drifts, shouldn't be trusted
    precisely over a long lookback).
  - src/backtest/outcome.ts: classifyOutcome. Deliberately does NOT attempt
    to detect "rugs" - the original feature_list.json wording asked for that,
    and this session concluded partway through implementation that it can't
    be defended the way this project defends everything else it claims
    (would need price history / holder-exit-pattern evidence this project
    has no verified source for, and Pons's own design already structurally
    blocks the classic vectors - vault-locked LP, zero deployer privilege -
    a rug check would look for). Reports graduated / abandoned (stalled,
    progress<5% after 7+ days) / active / indeterminate (too recent) instead,
    each with a real on-chain evidence string. feature_list.json's wording
    was corrected to match what's actually implemented and why, following
    this project's own precedent (session 4 did the same for the
    repo-fingerprint feature's steps) rather than silently shipping
    something narrower than originally specified without saying so.
  - src/backtest/backtest.ts: runBacktest replays each historical launch
    through the REAL scanLaunch and REAL judgeLaunch (catches judgeLaunch's
    failure per-entry into `verdictError`, doesn't let one blocked call stop
    the whole replay). summarizeBacktest cross-tabulates verdict category
    against actual outcome - unit-tested to confirm it never fabricates a
    count for an entry with no verdict (verdictsUnavailable is tracked
    separately and explicitly, the four category counts only ever sum over
    entries that actually have a verdict).
  - 7 new tests: 4 for classifyOutcome (pure logic, all passing), 2 for
    summarizeBacktest (pure logic, all passing, including the
    never-fabricates-counts case), 1 live-network test for
    findHistoricalLaunches against a real recent window.

  **That 7th test hit this session's own cumulative rate limit, not a bug.**
  By the time this feature was built, this session had already run a full
  day of manual verification scripts plus multiple complete test-suite runs
  against `rpc.mainnet.chain.robinhood.com` (sessions 3 through 9, culminating
  in the four.meme adapter work immediately prior) - on top of a genuine,
  separate `eth_getLogs` infrastructure outage on Robinhood Chain's own side
  that this session also hit and documented (session 8's notes: "internal
  server errror" typo in their own error message, then a leaked internal
  backend address in a timeout). The identical `getContractEvents` /
  `eth_getLogs` pattern this test uses was confirmed working correctly many
  times earlier in this same session (pons.test.ts, watch.test.ts, and
  extensive manual probing in sessions 3-9) before the cumulative load set
  in. Don't "fix" this by adding more retry logic - it's exhausted shared
  public-RPC capacity from a very long test session, not a code defect, and
  it will clear on its own once request volume against that one endpoint
  drops. If it's still failing in a fresh session with a cold request
  history, that would be a different, more concerning signal - worth
  distinguishing the two before assuming either way.

## What's in progress
(none — every entry in feature_list.json has now been attempted at least once)

## What's next (recommended)
This project has now hit a real ceiling: 4 of 7 features are done and
verified, and the other 3 are ALL blocked on one single missing credential
(ANTHROPIC_API_KEY), not on remaining engineering work. There is no more
"pick the next feature" work to do productively without it. Concretely, in
priority order if/when a key becomes available:
1. Add a real ANTHROPIC_API_KEY to .env. Run `warden judge <token>` against
   a real launch (attach `--repo` if the launch actually links a GitHub)
   and read
   the actual output. Check specifically whether the 6 pieces of "known
   context" baked into judge's system prompt (session 5's notes list them)
   actually change its reasoning the way they're meant to, not just whether
   it returns valid JSON. Only then flip feature [2] to true, and only if
   the output genuinely holds up - if it's generic or ignores the context,
   fix the prompt before flipping anything.
2. Add TELEGRAM_BOT_TOKEN + TELEGRAM_CHAT_ID (a bot from @BotFather). Run
   `warden watch --once <token>` and confirm a real message arrives, reads
   correctly, and matches what the terminal output claims was sent. Flip [3].
3. Run `warden backtest --since <recent date>` for real and read whether the
   win-rate cross-tab is actually informative once verdicts stop being null -
   the summary logic is unit-tested against synthetic data, but nobody has
   looked at what it says about real judge output yet. Flip [6] once that's
   sane.
4. Feature [4] (execution) stays passes:false regardless of API key
   availability, on principle - see session 7's notes. Do not self-fund a
   wallet to close this out unless a human explicitly decides to, with funds
   they've chosen to risk.

## Notes for the next session
Runtime is Deno. Full command list now: scan (--launchpad, --normalized),
vet-repo, judge, watch (--once / polling / --live / trade flags), backtest
(--since). New this session: src/backtest/{outcome,discover,backtest}.ts and
their tests.

Everything from sessions 3-8's notes still holds (Pons/four.meme protocol
details, the two watch bugs, the execution-gate scope decision, the
RPC-reliability findings) - not repeated here, read the git log for full
rationale on anything that needs it; each commit message is written to stand
on its own.

Do not commit a real .env. Same credentials as session 8's list - nothing
new was added this session (backtest reuses RPC_URL and ANTHROPIC_API_KEY,
both already tracked).
