# Project: warden — agentic launch-vetting tool
# Last updated: 2026-09-06 by session 5 (agent-judge, blocked on credential)

## What's done
- On-chain reader [index 0] and repo fingerprint [index 1] — unchanged this session.
- Agent-judge [feature_list.json index: 2] — IMPLEMENTED, still passes:false. This is
  the first feature this project has built without full end-to-end verification, and
  that's deliberate, not an oversight: this environment has no ANTHROPIC_API_KEY
  (checked with `env | grep -i anthropic` — not set; there's an ANTHROPIC_BASE_URL
  from the Claude Code session itself, but that's a separate credential scope and
  not something to reach for here). Per this project's own standing rule (session 3's
  audit, session 4's fingerprint work), passes only flips to true after a real
  end-to-end run — not just code that compiles and a test suite that passes.

  What IS verified: src/judge/judge.ts builds a request against
  @anthropic-ai/sdk (pinned ^0.123.0 — note 0.124.0 exists but was rejected by
  Deno's own minimum-dependency-age supply-chain policy for being <24h old at
  install time, a policy this project should keep respecting rather than
  bypassing with --min-dep-age), with a forced tool_choice on a record_verdict
  tool schema so the output shape (category/confidence/summary/signalsWeighed/
  reasoningLog) is structurally guaranteed rather than hoped-for prose-parsing.
  `warden judge <token> [--repo <owner>/<repo>]` runs the REAL scan (and real
  vet-repo, if --repo given) pipeline, then fails with a clean, specific error
  ("ANTHROPIC_API_KEY is not set - add it to .env before running judge") at the
  model-call gate — reproduced live against a real launch found on-chain this
  session (0x9308Ce1111c1e2B5d2C1a00b0Fc05cD5d67D49E7), exit code 1, no crash.
  saveVerdictLog is tested directly (writes verdicts/<token>-<timestamp>.json,
  reads back correctly) since it doesn't need the API. judgeLaunch's
  no-API-key error path is also tested directly. 9/9 tests passing overall
  (3 chain + 2 fingerprint + 2 retry + 2 judge).

  What is NOT verified: the actual model call. Never ran once. Unknown until
  someone adds a real key: whether the model reliably calls record_verdict
  (tool_choice should force this, per Anthropic's docs, but "should" isn't
  "observed"), whether signalsWeighed entries actually cite real fields the way
  the system prompt demands, whether the five pieces of "known context" baked
  into the system prompt (lpLocked pre-graduation, contractAddressMentions vs
  tickerMentions, snipeTaxBps=0 caveat, devBuy/holders null-vs-zero) actually
  change the model's behavior the way they're meant to, or just get ignored.

## What's in progress
(none — judge is done as far as it can go without a credential only the user can add)

## What's next (recommended)
- To unblock judge: add a real ANTHROPIC_API_KEY to .env, run
  `deno task start judge <token>` against a live launch (or `--repo Phosphenq/bodkin`
  against the launch that inspired this whole project, if it's still live, for a
  meaningful test case with real red flags to weigh), read the actual verdict JSON
  and the saved reasoning log, and only then flip index 2 to true - or fix whatever
  the real output reveals is wrong first.
- Telegram alerts [feature_list.json index: 3] is next in priority order regardless
  - likely to hit the same shape of blocker (needs a real TELEGRAM_BOT_TOKEN +
  TELEGRAM_CHAT_ID this environment doesn't have) - implement it the same way judge
  was handled: build it fully, verify everything that doesn't need the live
  credential, fail cleanly and specifically at whatever does, be honest about what's
  actually been exercised versus what's just "should work."

## Notes for the next session
Runtime is Deno. New this session: src/judge/judge.ts, src/judge/judge.test.ts.
`verdicts/` is gitignored (runtime output, not source) - added to .gitignore this
session.

Standing pattern across this whole project, reapplied again here: don't claim more
than what was actually run. A feature that's fully coded, type-checks, and has
passing tests for everything reachable without a missing credential is still
"not done" if the one thing it exists to do has never actually executed. This is
the same instinct as session 3's audit correction and session 4's contractAddress-
vs-ticker split, just applied one step earlier this time - catching it before
ever claiming passes:true, instead of an audit catching it after.

Everything from sessions 3-4's notes still holds (chain addresses, the snipe-tax
bytecode-vs-repo discrepancy, holder-concentration fix, GitHub star-timing
restriction) - not repeated here, read the git log if it's been a while.

Do not commit a real .env. If a session after this one DOES get a real
ANTHROPIC_API_KEY to test with, that key still must only ever live in .env
(gitignored), never in a commit, never printed to a log file that might get
committed.
